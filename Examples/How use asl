Este archivo contiene un código de un ejmeplo de como usar la función latin_square() de asl.py

N = 3
# Initialize the square with zeros
square = [[0 for _ in range(N)] for _ in range(N)]
# Call the main function to generate Latin squares
TS = latin_square(square, 1) 
# Print the total number of solutions found
print(f'Total solutions found: {TS}')
